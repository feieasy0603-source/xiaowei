spring:
  config:
    activate:
      on-profile: prod

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

# 前后端分域部署时设置，逗号分隔，例：https://example.com,https://example.com/admin
# 同域 Nginx 反代可留空
# xiaowei.cors.allowed-origins 由 CORS_ALLOWED_ORIGINS 注入

xiaowei:
  auth:
    demo-enabled: false
  ai:
    mock: false
    provider: openai
  payment:
    mock-enabled: false
    wechat:
      enabled: ${WECHAT_PAY_ENABLED:false}
      app-id: ${WECHAT_APP_ID:}
      mch-id: ${WECHAT_MCH_ID:}
      api-v3-key: ${WECHAT_API_V3_KEY:}
      serial-no: ${WECHAT_SERIAL_NO:}
      private-key-pem: ${WECHAT_PRIVATE_KEY_PEM:}
      notify-url: ${WECHAT_NOTIFY_URL:}
    alipay:
      enabled: ${ALIPAY_PAY_ENABLED:false}
      app-id: ${ALIPAY_APP_ID:}
      private-key-pem: ${ALIPAY_PRIVATE_KEY_PEM:}
      alipay-public-key-pem: ${ALIPAY_PUBLIC_KEY_PEM:}
      gateway-url: ${ALIPAY_GATEWAY_URL:https://openapi.alipay.com/gateway.do}
      notify-url: ${ALIPAY_NOTIFY_URL:}
  wallet:
    allow-direct-recharge: false
